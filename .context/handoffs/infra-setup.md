# Implementation: reusable setup and actions

You were spawned by the Drove orchestrator. Direct execution: implement/review now. Read AGENTS.md and the pinned contract /Users/jjmartin/Development/event-log/.context/handoffs/infra-contract.md. Do not append to the log. Do not run git commit, push, publish, or operate live reactors. Do not run doctor --fix/init in production; the controller handled preflight. Do not spawn agents. Stay inside your own worktree and claimed paths. If a dependency or scope change is needed, stop the dependent edits and report a concrete seam in your Herdr response; continue independent work. Reports go in your Herdr response (no collaboration tools or messages to other agents). Include changed paths, checks and results, limitations, and exact integration instructions. Keep going through fixes requested by the controller; do not announce completion with untested draft work.

Worktree: /Users/jjmartin/Development/event-log/.worktrees/infra-setup, branch feat/reusable-setup. Model gpt-5.6-terra/high. Own only the infra-setup paths in the contract. Implement the reusable setup/maintenance capability and packaged commit/doc actions plus lifecycle support upstream. Design a small coherent CLI (for example setup preview/apply and reusable action/lifecycle entry points); pick exact names and report them early. Ship assets in the Rust binary or skill, not only in this repository's .context/bin. Existing native react remains the supervisor. Keep per-project config declarative and invocation portable; do not interpolate untrusted path/setting strings into shell commands. Existing .context/eventlog.toml can be preserved; a separate owned config is acceptable if justified. Conflict detection must happen before partial application. Preserve user-owned Drovefiles and marker content. Configurable model commands enable stub integration tests. Tests may use disposable git repos and commit there as fixtures only. Put tests in your claimed existing test files and modules; no Cargo dependency edits (request them from controller/runtime owner). Runtime worker fixes native process/action accounting without changing public interfaces. Do not recreate that runtime here. Update distributed skill with usable setup/upgrade instructions and truthful caveats. Start by reporting your proposed CLI/settings contract, then continue implementation.
