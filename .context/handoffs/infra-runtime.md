# Implementation: native reactor runtime

You were spawned by the Drove orchestrator. Direct execution: implement/review now. Read AGENTS.md and the pinned contract /Users/jjmartin/Development/event-log/.context/handoffs/infra-contract.md. Do not append to the log. Do not run git commit, push, publish, or operate live reactors. Do not run doctor --fix/init in production; the controller handled preflight. Do not spawn agents. Stay inside your own worktree and claimed paths. If a dependency or scope change is needed, stop the dependent edits and report a concrete seam in your Herdr response; continue independent work. Reports go in your Herdr response (no collaboration tools or messages to other agents). Include changed paths, checks and results, limitations, and exact integration instructions. Keep going through fixes requested by the controller; do not announce completion with untested draft work.

Worktree: /Users/jjmartin/Development/event-log/.worktrees/infra-runtime, branch fix/action-runtime. Model gpt-5.6-terra/high. Own only infra-runtime paths in the contract. Fix demonstrated native runner pipe deadlock, timeout scope including blocked stdin and descendant-held stdout, descendant cleanup, diagnostics, and Git status accounting. Preserve public signatures and existing command/environment/outcome contract. Handle platform differences explicitly (crate supports macOS/Linux/Windows). Add meaningful regression tests with bounded outer guards to avoid hanging CI; include large output, children that outlive parent/retain pipe handles, timeout cleanup, stdin backpressure, existing outcome file/stdout semantics, exact unusual Git paths and dirty changes. Dependencies may be added minimally within Cargo files, no package/version/release edits. Coordinate requested dependency changes through controller. Setup worker owns reusable one-pass model actions and registration, so avoid duplicating that work. Provide a tested working-tree diff, no commits.
