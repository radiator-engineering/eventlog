# Independent review and acceptance

You were spawned by the Drove orchestrator. Direct execution: implement/review now. Read AGENTS.md and the pinned contract /Users/jjmartin/Development/event-log/.context/handoffs/infra-contract.md. Do not append to the log. Do not run git commit, push, publish, or operate live reactors. Do not run doctor --fix/init in production; the controller handled preflight. Do not spawn agents. Stay inside your own worktree and claimed paths. If a dependency or scope change is needed, stop the dependent edits and report a concrete seam in your Herdr response; continue independent work. Reports go in your Herdr response (no collaboration tools or messages to other agents). Include changed paths, checks and results, limitations, and exact integration instructions. Keep going through fixes requested by the controller; do not announce completion with untested draft work.

Worktree: /Users/jjmartin/Development/event-log/.worktrees/infra-review, branch review/reusable-infra. Model gpt-5.6-sol/high. Product code is read-only. You may write only .context/reports/infra-review.md in your worktree; create its parent directory there. Tests and integration copies may be created in a temporary directory outside all live repositories. Worker diffs are visible at sibling infra-setup and infra-runtime worktrees; do not commit or modify their files. First audit the pinned design/current code and archived findings, report concrete high-impact missing acceptance gates early. Then independently inspect worker changes as available. Build a disposable combined copy for testing once both diffs exist (ask controller for integration readiness if needed); never mistake early partial work for a final review. Validate fresh setup and disposable Drove acceptance, upgrades/customizations, real native-react stubbed commit/docs loop, staged index isolation, modified pre-dirty docs, lifecycle restart claims, bounded descendant/stdout behavior, and resume. Validate only copies; do not run Drove up or alter production sessions. Existing native react test runs actions, so temporary repos only. Final verdict APPROVE or CHANGES_REQUESTED must identify exact reviewed tree/diff evidence and commands/results. No GitHub comments or external messages needed at this stage.
