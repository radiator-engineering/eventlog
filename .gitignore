# coordination event log (event-log-coordination)
.context/events.jsonl
.context/events.jsonl.lock
.context/*.reactor.lock/
.context/layout.json
.DS_Store
target/

# cargo-mutants output
mutants.out*/

# Obsidian vault state (per-machine editor settings)
.obsidian/
